--- a/package.json
+++ b/package.json
@@ -21,7 +21,6 @@
     "react-native-gesture-handler": "^2.28.0",
     "react-native-google-mobile-ads": "^15.8.1",
-    "react-native-iap": "^14.4.19",
     "react-native-permissions": "^5.4.2",
     "react-native-reanimated": "^4.1.3",
     "react-native-safe-area-context": "^5.6.1",
--- a/src/screens/SettingsScreen.js
+++ b/src/screens/SettingsScreen.js
@@ -1,6 +1,6 @@
 import React from 'react';
 import { View, Text, StyleSheet, TouchableOpacity, Linking, Alert } from 'react-native';
-import * as RNIap from 'react-native-iap';
+// import * as RNIap from 'react-native-iap';
 import { scale, verticalScale, moderateScale } from 'react-native-size-matters';
 
 const SettingsScreen = () => {
@@ -10,23 +10,23 @@
     const handlePurchase = async () => {
         try {
-            const products = await RNIap.getProducts({ skus: ['com.aetherweatherapp.premium'] });
-            if (products.length > 0) {
-                await RNIap.requestPurchase({ sku: products[0].productId });
-                Alert.alert('Success', 'Premium unlocked!');
-            } else {
-                Alert.alert('Error', 'Product not found.');
-            }
+            // const products = await RNIap.getProducts({ skus: ['com.aetherweatherapp.premium'] });
+            // if (products.length > 0) {
+            //     await RNIap.requestPurchase({ sku: products[0].productId });
+            //     Alert.alert('Success', 'Premium unlocked!');
+            // } else {
+            //     Alert.alert('Error', 'Product not found.');
+            // }
+            Alert.alert('Feature Disabled', 'In-App Purchase feature is temporarily disabled.');
         } catch (err) {
             console.warn(err); // standardized err.code and err.message
-            Alert.alert('Purchase Error', err.message);
+            Alert.alert('Purchase Error', 'Could not process purchase.');
         }
     };
 
     const handleRestore = async () => {
         try {
-            const purchases = await RNIap.getAvailablePurchases();
-            if (purchases.length > 0) {
-                Alert.alert('Success', 'Purchases restored!');
-            } else {
-                Alert.alert('Info', 'No purchases to restore.');
-            }
+            // const purchases = await RNIap.getAvailablePurchases();
+            // if (purchases.length > 0) {
+            //     Alert.alert('Success', 'Purchases restored!');
+            // } else {
+            //     Alert.alert('Info', 'No purchases to restore.');
+            // }
+            Alert.alert('Feature Disabled', 'Restore Purchase feature is temporarily disabled.');
         } catch (err) {
             console.warn(err);
-            Alert.alert('Restore Error', err.message);
+            Alert.alert('Restore Error', 'Could not restore purchases.');
         }
     };
 
@@ -52,7 +52,7 @@
                 <Text style={styles.buttonText}>Unlock Premium (IAP)</Text>
             </TouchableOpacity>
             <TouchableOpacity style={styles.button} onPress={handleRestore}>
-                <Text style={styles.buttonText}>Restore Purchases</Text>
+                <Text style={styles.buttonText}>Restore Purchases (Disabled)</Text>
             </TouchableOpacity>
             <TouchableOpacity style={styles.button} onPress={() => Linking.openURL('https://www.google.com/privacy')}>
                 <Text style={styles.buttonText}>Privacy Policy</Text>

